export type MatrixNote = {
  readonly id: string;
  readonly calculationId: string;
  readonly ownerUserId: string;
  readonly text: string;
  readonly resultChecksum: string;
  readonly createdAt: string;
  readonly updatedAt: string;
};
